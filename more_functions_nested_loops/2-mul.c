#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * mul - multiplies two int
 * @a: first int to mul
 * @b: second int to mul
 *Return: 0
 */

int mul(int a, int b)
{
	int r = a * b;

	return (r);
}
