#include "main.h"

/**
 *_strlen - return the length of a string.
 *
 *@s: string
 * Return: the length of a string
 */


int _strlen(char *s)
{
int len = 0;

while (*s != '\0')
	_putchar(len);
	len++;


return (0);
}
