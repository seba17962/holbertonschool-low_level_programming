#include "main.h"

/**
 *_strlen - return the length of a string.
 *
 *@s: string
 * Return: the length of a string
 */


int _strlen(char *s)
{
int len = 0;
char str[*s];

len = _strlen(str);
_putchar(len);


return (0);
}
