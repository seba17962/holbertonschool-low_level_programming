#include <stdio.h>

int
main(void)
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
	putchar(alphabet);
	if (alphabet == 'q' || alphabet	== 'e');
	{
		continue
	}
}
putchar('\n');
return (0);
}
